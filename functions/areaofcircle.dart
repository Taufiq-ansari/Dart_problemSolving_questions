void main() {
  double pi = 3.14;
  int r = 2;
  double values = areaofCircle(pi, r);
  print(values);
}



// Write a program in Dart that find the area of a circle using function. Formula: pi * r * r
areaofCircle(double pi, int r) {
  return pi * r * r;
}
